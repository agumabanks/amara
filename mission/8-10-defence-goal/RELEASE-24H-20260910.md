# Amara OPPO release observation

Owner requested shorter, less intrusive group marketing and a release build for a 24-hour learning run. Existing uncommitted work is preserved.

## Group copy

Automatic group promotions now default to a compact caption: title, truthful price, one short fact (up to 85 characters), canonical offering link, and a brief private-message invitation. Closing lines vary deterministically by queued work identity. No invented urgency, promises, hashtags, or extra follow-up messages are added. Detailed copy is opt-in (up to 240 description characters); automatic group ads do not request it. Existing bound attempts are not rewritten, and uncertain actions are not retried. Existing owner-configured schedules, group topic filters and consent remain authoritative. This improves message size; it does not certify engagement or reduce configured frequency.

Illustrative shape:

> Office chair
> UGX 120,000
> Blue fabric, adjustable height.
> [canonical product link]
> Interested? Message us to confirm availability.

## Observation

The existing EvaluationJournal automatically begins a bounded 24-hour window once per installed release. Restarting the app does not reset the window. The private JSONL file persists scheduling, work outcomes, external-effect verification states, device state, policy/budget deferrals and caption-size/style observations. Storage is capped at 32 MB; a log at the cap may cover less than 24 hours. Existing durable transaction records remain authoritative. Queue completion alone is not proof of delivery. No credentials or caption bodies are added by the new caption event.

The shell-only receiver (protected by Android DUMP permission) supports read-only EXPORT_OBSERVATION in release. Debug testing/action hooks remain disabled. To export over **ADB USB** after the run:

```bash
scripts/observation/collect_24h.sh 7aef1a4c artifacts/amara-24h
```

This writes the JSONL files and a summary.json. Review VERIFIED versus UNCERTAIN external effects, failures, deferrals, caption lengths, and actual elapsed coverage. Do not infer customer satisfaction, sales, or 24-hour reliability from task starts or build success.

## Build identity

The release variant is optimized, resource-shrunk and non-debug. The dedicated OPPO currently uses the development signing identity; `-PdeviceContinuitySigning` retains that identity for replace-install without erasing data. Normal distribution signing remains the configured release key. This is a device observation release, not a claim of store-distribution signing readiness. The 15% battery cutoff remains active.

Validation and live certificate follow below.

Validation: 45 targeted Android tests passed (zero failures/errors/skips), including compact caption facts/variants/detail opt-in, 24-hour window persistence/export, and scheduler battery behavior. All 20 Flutter tests passed. Existing full-suite baseline from the preceding run remains 840 tests / 3 failures, documented in RELIABILITY-SOCIAL-20260910.md; those unrelated failures were not resolved or relabelled. The local pre-release snapshot contains 613 entries and is owner-readable only; it was taken while the app was running, so it is supplementary evidence rather than an atomic database backup.

## Installed release and live logger verification

Release assembly including shrinking and release validation passed. Installed with `adb install -r`, preserving app data. APK: `artifacts/reliability-social-20260910/Amara-24h-release.apk`. SHA-256: `50942f962c3d233abd85cd71c014cae8408698c9d5ccc55808b7c882622ebb53`. Installed hash matched; APK is non-debuggable and retains the existing device signing certificate. Certificate: `artifacts/reliability-social-20260910/release-certificate.txt`: Accessibility enabled/bound/not crashed, overlay allowed, notification listener and battery exemption present, AgentService foreground, 63 scheduler matches, zero recent fatal/ANR evidence.

The first real release export succeeded over ADB USB, with zero malformed JSONL lines. Window: **September 10, 15:51:45 EAT through September 11, 15:51:45 EAT**. Evidence: `artifacts/reliability-social-20260910/release-first-observation/`. Initial world evidence: battery 16%, thermal NORMAL, owner inactive, network available. Initial logs show existing circuit-breaker deferrals for TikTok publishing and one WhatsApp broadcast work key. No fresh verified external effect is present in that initial sample. Do not claim these workflows or the full mission complete. The release is left running with the owner's schedules and uncertainty protections intact.

## Community investigation and correction

Owner reported no visible TikTok commenting. Release log inspection confirms no community task started in the sampled release period; no TikTok public-comment external effect was recorded. Five WhatsApp tasks ran (four skipped, one verified delivery), then battery reached the owner-selected 15% cutoff. Accessibility remained bound. Historical pre-release snapshot contains 6 VERIFIED / 9 UNCERTAIN / 23 OBSERVED records; these historical totals do not establish a new visible comment or validate the current UI path.

Two concrete code defects were found: (1) the community priority query treated a pending circuit-broken TikTok publication as ready priority work; (2) read-only reconciliation of one old uncertain comment returned from the entire community session, preventing fresh discovery. The correction excludes circuit-broken kinds from the community priority query and continues discovery after bounded reconciliation. Existing reserved/uncertain posts remain ineligible for replay. Stage events record discovery, post reads and model action/relevance; final events distinguish blocked/no-relevant-post/verified/uncertain outcomes without logging comment text. Live end-to-end completion remains unverified until battery permits a session.

Community correction deployed with `install -r`: 50 targeted tests passed, release assembly passed, `git diff --check` passed. Superseding APK SHA-256 `36662b9c67b0a4e1f74241b98401067c0d8df3c50a61d1f0cbf5d1a1d610236f`; installed hash matched. Certificate `artifacts/reliability-social-20260910/community-certificate.txt`: Accessibility bound/not crashed, overlay/listener/battery exemption/foreground service pass, 65 scheduler matches, zero recent fatal/ANR. Read-only release log export passed. New release window ends **September 11 at 16:14:27 EAT**; previous exported observation is retained. Live battery remains 15% on USB and the new build records session_budget_deferred, so discovery-to-comment completion remains NOT VERIFIED. No uncertain comment was replayed.

## Later live investigation: TikTok and ellipsized WhatsApp names

Device transport changed: wireless ADB `192.168.1.65:37947` reports hardware serial `7aef1a4c`, AC-powered at 77%; this supersedes the earlier USB/low-battery condition. Accessibility remains bound/not crashed. Exported release evidence is `artifacts/reliability-social-20260910/latest-investigation/`.

Observed community outcomes include VERIFIED at 16:47:07 EAT, UNCERTAIN at 17:02:03, five profile-identity failures, and several NO_RELEVANT_POST sessions. External-effect records corroborate one verified and one uncertain public comment. Publishing has nine attempted effects: five UNCERTAIN and four FAILED, zero VERIFIED in this sample. These uncertain attempts must not be replayed.

Read-only live profile inspection proved @sanaamedia / Sanaa media, and current published grid tiles use `com.zhiliaoapp.musically:id/f0t`, absent from the previous newest-post verifier. Added the observed tile alias, retained draft exclusion and exact-caption checks, and replaced semantic Profile clicks with visible bounded tab taps in social and publication navigation. Added persisted TikTok preparation-stage failure events to distinguish import/sound/composer failures.

WhatsApp discovery previously saved displayed row text even when ending in ellipsis. New discovery omits truncated identities; trailing ellipsis is stripped only for search location, never accepted as identity. Matching can use an exact full accessibility description as well as row text. Truncated names, including two equal truncated labels, cannot authorize sending. Existing malformed saved names need their full canonical identities confirmed; no bulk rename or prefix-based sending was performed. Owner was asked for an affected full name.

Names/profile correction deployed: 45 targeted tests passed, release assembly and diff check passed. APK SHA-256 `f8f9c4cfe1cbfa09efecb61249de9cc452f56a91ca3a06a4d98ec0b78d58c7e6`; installed hash matched. Certificate `artifacts/reliability-social-20260910/names-tiktok-certificate.txt`: Accessibility bound/not crashed, overlay/listener/battery exemption/foreground service pass, 60 job matches, no recent fatal/ANR. Release export passed; replacement observation window ends 2026-09-11T21:18:13.564000+03:00. Fresh publication success and affected WhatsApp full-name repair remain unverified; no uncertain attempts were replayed.
