# Queue admission and operational health — September 13

The installed predecessor's journal proved a live loop repeatedly declining TikTok admission during quiet hours with only 120 seconds left in its screen budget. That is not a model failure or a delivery receipt.

This change scans up to 50 candidates, durably delays each inadmissible item for 60 seconds without claiming it or consuming an attempt, and considers other work. Background work no longer consumes the screen-budget admission allowance. Every item inside a session is rechecked so a background or emergency-reply grant cannot authorize a different screen task. SafetyGovernor applies the daily screen cap only to work requiring SCREEN. Deferred items retain normal policy and delivery-ledger checks.

Health monitoring now runs every five minutes in the service watchdog, with the existing independent periodic worker as backup. Loop recovery starts a job only if the previous job is inactive, otherwise wakes the existing loop; it does not cancel active work, clear holds or override owner halt. Recovery requests and health sweeps enter the durable observation journal. Actual process death still relies on Android service/worker scheduling; it is not a guarantee against force-stop or OS restrictions.

Owner-facing health adds near-cutoff battery warning (16–20% when not charging), insufficient remaining screen budget, owner halt, missing Groq credentials and up to five held group schedules with reasons. Health sweeps are serialized. Manager-warning conditions use normalized signatures and per-condition six-hour cooldowns plus a five-minute global spacing. Time-scoped work keys permit later reminders instead of a permanent completed-key tombstone. WhatsApp warnings need a working channel and are never called delivered before verification. Local warnings remain available when WhatsApp cannot send.

Chat refreshes operational status and displays full blocker text. Exact local questions such as “are you working”, “what is blocking you”, “device status”, and “queue status” read current health/queue state without a model call. Existing navigation is preserved.

Backend configuration inspection found three numbered legacy GROK_API_KEY variables, while only an unnumbered primary and key 2 were exposed. `/var/www/cards.sanaa.ug/config/services.php` now resolves GROQ or legacy GROK numbered keys 1–16 and optional GROQ_API_KEYS; registration/config responses include the existing Android `groq_api_keys` field. PHP syntax checks and configuration cache refresh passed; bootstrapped configuration reports three unique pool keys and a configured primary. No key values were printed or changed. Original backend files were backed up privately under `/root/amara-backups/blockage-20260913`. Keys sharing a provider quota do not create extra quota.

The first regression run caught an invalid test fixture: periodic TikTok publication entries compact to one pending item, so the proposed 60-item admission-bound test did not contain 60 items. The fixture now uses independent broadcast work across domain capacities. This was a test setup failure, not reported as a passing run.

Live Naalya delivery, verified TikTok follow-up/comment completion, manager-warning delivery and a sustained full-day run still require fresh evidence. Release private app data cannot be read with run-as because the production build is not debuggable; this was not bypassed.

Validation: 94 targeted Android tests now pass (46 autonomous work integration, 44 model gateway, 2 foreground service and 2 runtime-status tests). Coverage includes ordinary admission blockage, expired-lease blockage without rearming, the 50-candidate bound, exhausted screen budget with internal health still allowed, and ten-key failover. Flutter chat analysis and diff whitespace checks pass. A further intermediate run included the new expired-lease test before its matching production-class rebuild and failed that case; the completed rebuild passes it. These are targeted results, not a full-suite claim.

The source pool mapping was also checked with ten synthetic legacy numbered keys and mixed separators without using or printing real credentials. A read-only backend search returned no Naalya-named log summaries, so no new Naalya receipt is inferred from backend data.

## Device follow-up

Installed release SHA-256 `049694b58eba9dd2ccbd16cbdb8fb8b94a3f2e2b314804217188b716ec41824f` matched the device. Full readiness certificate passed (Accessibility bound/not crashed, overlay, listener, battery exemption, foreground service, scheduler presence, no recent fatal/ANR matches). Fresh build segment `amara-release-observation:1789252349568` proves TikTok admission deferred for screen budget, followed by MARKET_ANALYSIS DONE and two operational health observations. This is live evidence the blocked head no longer prevents background work.

Read-only owner-chat inspection exposed five held groups: BandaChristianFellowship (picker_next); Business Centre Uganda and Naalya E-Trade (soko24.co name resolution); MSA-KcU and Software Remote Jobs (recipient matching). The device also reported low remaining daily screen time and cooling-down work kinds. Backend health logs were stale August records; absence of fresh telemetry is not evidence of a dead device (backend logging is opt-in). Accessibility temporarily disconnected/reconnected in the sampled service log; the Android bound-services certificate remains a separate observation.

Follow-up corrections: constrain the blocker panel to 18% of screen height with independent scrolling so it cannot consume the composer; a health inspection finding problems completes as queue work while retaining its unhealthy module verdict; explicitly classified catalogue IOException before dispatch defers the group five minutes without increasing delivery-failure holds. Generic historical exceptions and already-held/uncertain work are not automatically resumed. New regression verifies repeated pre-send failures do not pause a schedule, but cannot clear an existing uncertain hold. Follow-up build/device validation remains to be recorded.

Follow-up installed and certified: SHA-256 `b8a07f097123827a54913d0372dd26014472ec027b307c03e5890fad23a017a4`, PID 17948, release 0.10.0 (13), replace-install with preserved data. Accessibility bound/not crashed, overlay and notification listener allowed, battery exemption and foreground service present; no recent fatal/ANR matches. Evidence: `health-followup-install.txt`, `health-followup-settled.txt`, `health-followup-device.txt` under `artifacts/amara-24h-20260913`.

49 targeted follow-up tests passed (46 autonomous integration + 3 group settings); Flutter analysis and whitespace checks passed. Fresh journal segment `amara-release-observation:1789253240414` shows two unhealthy operational inspections and INTERNAL_HEALTH_CHECK DONE with no failure class: finding blockers no longer fails queue execution. No external delivery is inferred. Live UI dump `health-chat-fixed.xml` confirms attachment control, editable composer and Send within visible bounds [131,1314][572,1422] / [590,1314][698,1422], with an independently scrollable blocker panel. Seven blockers still require attention; historical Naalya hold, TikTok completion and manager-warning delivery remain unresolved evidence requirements.
