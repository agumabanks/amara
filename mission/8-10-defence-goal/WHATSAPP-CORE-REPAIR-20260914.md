# OPPO WhatsApp core repair — 14 September 2026

## Observed before repair

OPPO 192.168.1.65:38493 had Amara 0.10.4 (17) and Terminal 2.0.4 (2029). The observation export through 13:43 UTC contained no WhatsApp ACTING or VERIFIED side effects. Opening WhatsApp was not proof of a send.

- 26 queued replies escalated because the exact destination was unavailable.
- Seven replies were refused by expired Terminal identity; four were rejected as unscoped.
- Seven inbound replies could not show the original conversation and message together; six could not reopen the original notification route.
- Group recovery repeatedly checked paused work without dispatching (131 skipped broadcast outcomes).
- Five TikTok publications were verified, while eight attempts failed on expired Terminal identity.

The composition-root business guard admitted only TikTok. WhatsApp wrappers did not supply shop scope. Manager reports reused the customer path, supplied no guard inputs, and attempted to match a numeric target to a saved-name conversation header. Live OPPO inspection confirmed the manager is a WhatsApp Business contact and its actual number appears under `com.whatsapp:id/business_subtitle`.

## Repairs under validation

- Signed Terminal demand refresh and reserved TikTok posting budget from the previous pending repair.
- Narrow WhatsApp channel admission with scope checks at both transaction boundaries.
- Customer memories and reply drafts keyed by shop; catalogue fetched from the current signed shop, with a second scope check before composing.
- New group ads bound to shop identity. Unscoped drafts can only be rebuilt if no external transaction exists. Uncertain dispatches remain held.
- Manager alerts admitted only for the configured manager destination and internal report provenance, even when Terminal cannot refresh.
- Phone destinations opened through WhatsApp's phone link, then checked against an actual contact-profile phone field. Saved names alone do not prove phone identity.
- Stable notification-route keys and current-message checks; no guessed-name fallback for missing origin tokens.
- Direct manager command candidates require a WhatsApp-owned notification, live exact manager phone, and the original visible message before entering the existing owner-command controller. Persistent claims prevent blind replay after interruption.
- Manager results, observed Terminal bookings/alerts and scoped paid/delivered order transitions queue deduplicated notifications. Queueing is reported as queueing, not delivery.
- Manager command/report circuit breakers isolated from customer failures. A manager delivery cannot clear a different customer conversation's blocker.
- Queued manager and scheduled commands reuse the work loop's existing device lease; the owner controller no longer tries to acquire the same non-reentrant mutex twice.
- Terminal Alerts checks recognise the current Needs action surface instead of waiting for the removed Inbox title. Two real bookings were observed on OPPO during the read-only inspection.
- Unscoped pre-dispatch group work is retired into a new occurrence key, preserving immutable old media bindings and preventing reuse of another shop's artwork.

## Audio follow-up

The local video encoder produces video-only media. TikTok preparation waits 15 seconds for automatic soundtrack assignment but explicitly treats sound selection as optional. No soundtrack-presence proof currently gates publication. This explains how successful posting can still produce a silent video. Audio remediation follows core deployment and delivery verification.

## Validation

Initial targeted run passed 104 tests with zero failures/errors. A final build/test run includes the subsequently discovered lock handoff, Alerts selector and blocker-destination repairs. Live delivery remains pending. Only OPPO is connected. TPS cannot be certified or updated while disconnected. No claim that every WhatsApp workflow or manager command has been verified yet.

## Live outcome at 17:30 EAT

Amara 0.10.7 (20) and Terminal 2.0.5 (2030) installed on OPPO. Amara remains On. Accessibility enabled/bound, no crashed service, overlay allowed, no recent fatal/ANR in deployment certificate.

The first 0.10.6 manager canary exposed an additional schema error (`manager_report` was not registered). This was corrected in the authoritative capability schema, with a full SideEffectRunner regression proving the configured notification dispatches once and duplicate invocation cannot resend. No schema bypass was introduced. Version 0.10.7 then delivered the manager update with VERIFIED evidence at 14:29:38 UTC / 17:29:38 EAT.

Two scheduled group sends reached VERIFIED at 14:24:51 and 14:26:05 UTC. A different group remains blocked on `recipient_not_unique`; this is not universal WhatsApp readiness. Historical review-held messages are not blindly replayed. Manager command return-path needs a real reply from the configured manager; the owner was asked to send a harmless status question.

Validation: 132 core regressions; 35 transaction tests plus two policy tests; 19 copy/conversation tests, all with zero failures. Deployment and redacted verified-side-effect evidence are in `artifacts/amara-reliability-20260914/`.

Audience communication now uses product-specific buying situations, catalogue facts and a relevant question. TikTok comments are instructed to answer concrete concerns without generic praise, invented experience, forced slang or unsupported promises. Copy has deterministic variants tied to the actual offering; this is not yet evidence of increased engagement or sales.

## Audio repair evidence

A diagnostic video-only clip reproduced the editor's `Add sound` state. Opening the picker and choosing a track is insufficient evidence until the picker closes and `tv_top_text` shows the exact chosen title with the remove-track control present. OPPO showed this transition from Add sound to Vula Umoya; the diagnostic was not published.

Version 0.10.8 now attempts selection through the observed TikTok picker and requires readback before advancing. An editor skipped by TikTok or an unverified selection records `sound_selection_unverified` / `sound_editor_skipped_unverified` and stops publication. This proves selected-track state, not end-to-end audible playback; platform muting/removal after publication is a separate possible outcome.

The final audio/copy/transaction regression selection passed 61 tests with no failures or errors. A second manager report was also verified on 0.10.7. A later TikTok attempt failed before dispatch at composer_timeout; publication reliability beyond the demonstrated sound-editor transition remains under observation. No manager reply has been received during this validation window.

## Final installation

Amara 0.10.8 (21), SHA-256 `b2251e949f0ce94d736b6edc0941e349ca82f637dd559d5c8c69acbeeedf7d82`, installed successfully on OPPO. Deployment helper confirms PID 24655, Accessibility enabled/bound with no crashed service, overlay allowed and no recent fatal/ANR. Diagnostic video removed from Downloads. No diagnostic clip was published. The audio workflow was validated against the real picker/editor, but a new automated publication with audible playback remains unverified. TPS was not connected and was not updated.
